# Slide Panel Checkout

## Описание
Премиальный слайд-панель для оформления заказов, который выезжает слева и занимает:
- **50% экрана** на десктопах (≥1024px)
- **100% экрана** на планшетах и телефонах (<1024px)

## Особенности

### 🎨 Дизайн
- **Премиальный минимализм** в черно-белой цветовой схеме
- **Шрифты**: Roboto Condensed, Raleway, Manrope, Satoshi, Ranade
- **Плавные анимации** с cubic-bezier кривыми
- **Адаптивная верстка** для всех устройств

### 📱 Адаптивность
- **Десктоп (≥1024px)**: 50% ширины экрана
- **Планшет (768px-1024px)**: 100% ширины, 40% высоты для корзины, 60% для формы
- **Телефон (≤768px)**: 100% ширины, 35% высоты для корзины, 65% для формы
- **Малый телефон (≤480px)**: 30% высоты для корзины, 70% для формы

### ⚡ Анимации
- **Выезд слева** с плавной анимацией
- **Затемнение фона** с blur эффектом
- **Плавное закрытие** с выездом обратно
- **Hover эффекты** на всех интерактивных элементах

### 🎯 Функциональность
- **Сохранена вся логика** работы с корзиной
- **Валидация форм** с красивыми сообщениями об ошибках
- **PhoneInput** с сохраненными стилями
- **CSRF защита** и безопасность
- **Закрытие по Escape** и клику по фону

## Использование

```jsx
import OrderModal from './components/orderwindow/modalwindow';

// В компоненте
const [showModal, setShowModal] = useState(false);

{showModal && (
  <OrderModal 
    cart={cart} 
    setCart={setCart} 
    onClose={() => setShowModal(false)} 
  />
)}
```

## Структура

```
modalwindow.js          # Основная логика
modalwindow.module.css  # Стили слайд-панели
```

## Преимущества

✅ **Современный UX** - слайд-панель вместо модального окна  
✅ **Премиальный дизайн** - минимализм и элегантность  
✅ **Полная адаптивность** - работает на всех устройствах  
✅ **Сохранена функциональность** - вся логика осталась  
✅ **Быстрая анимация** - плавные переходы  
✅ **Доступность** - поддержка клавиатуры и скринридеров 